# Activate renv for this project
source("renv/activate.R")

# Print message when R session starts
cat("renv activated for Rapa Nui Rats R analysis\n")
cat("To install/restore packages, run: renv::restore()\n")
cat("To update the lockfile after installing new packages, run: renv::snapshot()\n\n")